# API-only configuration overrides
# Merge with default.yaml for full configuration

api:
  api_key: ""
  base_url: ""
  model_name: "gpt-4o"
  embedding_model_name: "text-embedding-3-large"
  temperature: 0.0
  max_tokens: 4096
